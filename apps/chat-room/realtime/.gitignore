# Dependencies
node_modules/

# Build output (regenerated by `npm run build`)
dist/

# Local env files — never commit secrets. Heroku config vars are used in
# production instead.
.env
.env.local
.env.*.local
.env.production

# Keep the example (no secrets) tracked.
!.env.example
